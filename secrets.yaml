# secrets.yaml - Contains sensitive configuration
# This file should be added to your .gitignore!

exchange:
  api_key: "YOUR_ACTUAL_API_KEY"
  api_secret: "YOUR_ACTUAL_API_SECRET"
  # api_passphrase: "YOUR_ACTUAL_PASSPHRASE" # If needed

redis:
  # password: "YOUR_ACTUAL_REDIS_PASSWORD" # If needed